{
  "request": "Assess this proposal before we decide. Do not implement it or accept the ADR.",
  "artifacts": {
    "docs/proposal.md": "# Draft order events proposal\nCommit an order to the database, then publish its event to a queue. This guarantees exactly-once delivery because both services retry.\n",
    "docs/requirements.md": "No committed order may lose its event after a process crash. Consumers already deduplicate by order ID. No cross-service transaction mechanism or provider guarantee is documented. The ADR remains a draft; only the user can accept it."
  },
  "environment": "Manual simulated decision evaluation. Inspect only the supplied complete relevant excerpts and observations. No network, external tools, or live repository is available. Propose actions or patches; do not execute them or claim new test results. Skill availability is controlled separately by the evaluator.",
  "constraints": [
    "Design assessment only; no implementation or ADR acceptance.",
    "Provider documentation is unavailable; identify unverified guarantees rather than inventing them."
  ],
  "termination": {
    "condition": "Return one final answer completing the request or identifying a concrete unresolved fact.",
    "max_responses": 1
  }
}
